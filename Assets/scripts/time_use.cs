using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class time_use : MonoBehaviour
{
    // Start is called before the first frame update
    float a;

    void Start()
    {
 
    }

    // Update is called once per frame
    void Update()
    {
    
    }
}
